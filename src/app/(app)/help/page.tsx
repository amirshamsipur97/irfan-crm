import { HelpGuide } from "@/components/help/HelpGuide";

/** Help & guide — static trilingual manual for the CRM, opened from the TopBar "?". */
export default function HelpPage() {
  return <HelpGuide />;
}
